# Local-dev env. Copy to .env.dev (gitignored) and tweak.
# Used with: docker compose -f docker/docker-compose-all.yml -f docker/docker-compose.dev.yml --env-file .env.dev up

SPRING_PROFILES_ACTIVE=docker

# ELK
ELASTIC_USERNAME=elastic
ELASTIC_PASSWORD=changeme
LOG_ENVIRONMENT=development
ES_XPACK_SECURITY=false